<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinweizhao.mapper.SysUserMapper">

    <select id="selectUserInfoByUsername" resultType="com.qinweizhao.entity.SysUserDetails">
        SELECT user_id, username, password, status
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectPermissionByUserId" resultType="java.lang.String">
        SELECT permission
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
                 LEFT JOIN sys_role r ON rm.role_id = r.role_id
                 LEFT JOIN sys_user_role ur ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId}
    </select>


</mapper>
